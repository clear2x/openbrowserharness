# 安全

[English](SAFETY.md) | 中文

> 承接说明：本文件继承自上游 DeepSeek Harness 的 SAFETY.md，其中「本项目」同时指 harness 本体与在其上构建的 OpenBrowserHarness 扩展。

## 实验性状态

OpenBrowserHarness 是实验性的开发者预览软件。它尚未接受安全审计，不得视为安全或可用于生产环境的软件。

本项目可以执行模型生成的代码与命令、加载第三方插件，并访问向其开放的网络、进程、凭据和文件。错误的模型输出、缺陷、配置错误、恶意输入或不可信插件可能损坏宿主计算机、修改或删除文件、泄露数据或凭据，或造成其他非预期影响。

## 浏览器自动化风险（扩展）

本扩展驱动的是你的**真实浏览器**——与你日常共用同一份 profile、Cookie 和登录态。除上述桌面风险外还需注意：

- **登录状态下的不可逆操作。** 指向购物、银行或社交页面时，agent 可以以你的身份下单、发帖、发消息。审批提示能降低风险，但最终取决于点击的人。
- **目标网站的服务条款。** 即使每个动作单独无害，自动化交互也可能违反网站条款。使用方式的责任在使用者。
- **`chrome.debugger` 横幅可见。** 自动化连接期间 Chrome/Edge 会显示「已开始调试此浏览器」；如果横幅意外消失，请关闭该页面。
- **提示内容会离开本机。** agent 读到的页面内容（以及你输入的一切）会发送给你配置的模型供应商。除此之外没有任何收集——没有遥测——但请把 agent 看到的所有内容视为已向该供应商披露。

实操建议：先在一次性页面上试；信任一个任务前把权限档位保持在「每次确认」；优先使用独立浏览器 profile 而非日常 profile；事后导出会话日志审计实际发生了什么。

## 沙箱限制

沙箱、审批提示与权限控制可以降低风险，但不保证隔离，也不能保证防止损害。即使限制得到正确执行，也无法保护本项目获准访问的资源。

不要把 OpenBrowserHarness 当作不可信工作负载唯一的安全控制措施。

## 负责任地使用

- 仅向本项目授予所需的最小权限和访问范围。
- 优先在一次性虚拟机、容器或专用环境中运行。
- 备份本项目可以访问的文件。
- 除非你接受相关风险，否则不要向其暴露敏感凭据或数据。
- 在允许运行前检查插件、配置和拟执行命令。

## 不提供保证，不承担责任

请在充分了解相关风险的前提下使用 OpenBrowserHarness。本软件依照 [MIT License](LICENSE) 提供，不附带任何保证。在适用法律允许的最大范围内，对于使用本项目造成的计算机损坏、数据丢失或泄露、文件丢失或其他损害，作者和版权持有人不承担责任。
