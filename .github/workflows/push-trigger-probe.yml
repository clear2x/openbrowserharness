# Temporary diagnostic: a minimal push-triggered workflow to isolate whether
# push events reach workflow evaluation in this repository at all. Remove
# once the push-trigger anomaly is resolved.
name: push-trigger-probe

on:
  push:
    branches: [master]

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo "push trigger works"
